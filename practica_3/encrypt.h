#ifndef ENCRYPT_H
#define ENCRYPT_H
#include <string>

using namespace std;

void encrypt(); // Función principal
string firstMethod(const string &binary, int n);
string secondMethod(const string &binary, int n);

#endif // ENCRYPT_H
