#ifndef DECRYPT_H
#define DECRYPT_H
#include "string"

using namespace std;

void decrypt();
string decryptFirstMethod(const string &binary, int n);
string decryptSecondMethod(const string &binary, int n);

#endif // DECRYPT_H
